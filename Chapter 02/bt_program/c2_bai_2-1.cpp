#include <iostream>  // Include the input/output stream library.
// Use the standard namespace to avoid prepending std:: to everything from the standard library.
using namespace std; 
int main() {   // The starting point of the program is the main function.
    cout << "Programming is great fun!"; // Print the text to the console.
    return 0;  // Return 0 to indicate successful program execution to the operating system.
}
